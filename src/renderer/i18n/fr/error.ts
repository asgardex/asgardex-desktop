import { ErrorMessages } from '../types'

const error: ErrorMessages = {
  'error.chainflip.channelIdRequired': 'ID de canal requis pour le suivi Chainflip',
  'error.interact.missingData.title': 'Données manquantes pour InteractiveView'
}

export default error

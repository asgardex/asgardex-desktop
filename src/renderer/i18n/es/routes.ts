import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': 'Activo no válido en la ruta: {asset}',
  'routes.invalid.params': 'Parámetro(s) de ruta no válido(s) {params}'
}

export default messages

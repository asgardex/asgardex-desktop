import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': 'मार्ग में अमान्य संपत्ति: {asset}',
  'routes.invalid.params': 'मार्ग पैरामीटर में त्रुटि {params}'
}

export default messages

import { ModalMessages } from '../types'

const modal: ModalMessages = {
  // TODO: Need Russian translation by native speaker
  'modal.provider.selectProtocols.title': 'Select Protocols'
}

export default modal

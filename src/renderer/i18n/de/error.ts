import { ErrorMessages } from '../types'

const error: ErrorMessages = {
  'error.chainflip.channelIdRequired': 'Kanal-ID für Chainflip-Tracking erforderlich',
  'error.interact.missingData.title': 'Fehlende Daten für InteractiveView'
}

export default error

import { ErrorMessages } from '../types'

const error: ErrorMessages = {
  'error.chainflip.channelIdRequired': 'Channel ID required for Chainflip tracking',
  'error.interact.missingData.title': 'Missing data for InteractiveView'
}

export default error

import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': '경로에 유효하지 않은 자산: {asset}',
  'routes.invalid.params': '유효하지 않은 경로 매개변수: {params}'
}

export default messages
